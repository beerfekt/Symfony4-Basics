  /**
     * @Route("/articles/save", name="articles-save")
     */
    public function save(/*Request $request*/)
    {
        return $this->render('articles/articles_output.html.twig',
            ['controller_name' => 'ArticleController',
                //DEV: ausgabe hier zu testzwecken
                'articles' => $this->generateTestArticleArray() ]
        );

    }


    private function generateTestArticleArray() {
        //array mit artikeln generieren (develop-test)

        //db #1
        $entityManager = $this->getDoctrine()->getManager();

        $anzahl = 3;
        $articles = array();

        for ($i = 0; $i < $anzahl; $i++)
        {
            $currentArticle =  new Article();
            $currentArticle->setTheTitle('TEST');
            $currentArticle->setTheBody('Hey du arsch, pflaume, depp!!' . $i);
            #db #2
            $entityManager->persist($currentArticle);
            $entityManager->flush();
            //DEV: abspeichern zu testzwecken und ausgabe hier
            $articles[$i] = $currentArticle;
        }

        return $articles;
    }







{% extends 'base.html.twig' %}

{# Title wird je Seite überschrieben #}
{% block title %}
    Hello  {{ app.request.query.get('name') | default('CodeReviewVideos') }}!
{% endblock title %}


{% block body %}

    {# if there are articles delivered from the admin zone... #}
    {% if articles %}
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Article Title</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {# loop through the $articles (in ArticleController.php) and show them #}
                {%  for article in articles %}
                    <tr>
                        <td>

                            {{ article.title | default('NO value received - from: ') }} {{ controller_name }}
                        </td>
                        <td>
                            <a href="/article/1" class="btn btn-dark">Show Article</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}

    {% endif %}

    {{ dump() }}

{% endblock %}